# Minimal runtime/training dependencies for CoupletAI
# Note: Installing PyTorch is platform/CUDA specific; follow https://pytorch.org/get-started/locally/
# and then install the rest via: pip install -r requirements.txt

flask>=2.0
nltk>=3.8
tensorboard>=2.12
tqdm>=4.60
